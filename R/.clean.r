#'
#'
#'


# consider moving to .utils unless it gets too big
.clean <- function(data, fixed, random)
{
  print("the data are now clean")
  # add functions to check and clean data
  return(data)
}
