# general
if(1==2)
{
  ids          <- t1$ids
  dv           <- t1$dv
  time         <- t1$time
  phase        <- t1$phase
  ivs          <- t1$ivs
  interactions <- t1$interactions
  time_power   <- t1$time_power
  correlation  <- t1$correlation
  family       <- t1$family
  fixed        <- t1$fixed
  random       <- t1$random
  formula      <- t1$formula
  method       <- t1$method
}

# formula
if(1==2)
{
  ids          = NULL
  dv           = NULL
  time         = NULL
  phase        = NULL
  ivs          = NULL
  interactions = NULL
  time_power   = NULL
  correlation  = NULL
  family       = t1$family
  fixed        = NULL
  random       = NULL
  formula      = formula
  method       = t1$method
}
